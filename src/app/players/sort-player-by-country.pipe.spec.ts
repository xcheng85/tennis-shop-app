import { SortPlayerByCountryPipe } from './sort-player-by-country.pipe';

describe('SortPlayerByCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPlayerByCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
