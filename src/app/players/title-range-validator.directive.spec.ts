import { TitleRangeValidatorDirective } from './title-range-validator.directive';

describe('TitleRangeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleRangeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
