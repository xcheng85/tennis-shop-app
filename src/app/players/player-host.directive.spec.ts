import { PlayerHostDirective } from './player-host.directive';

describe('PlayerHostDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayerHostDirective();
    expect(directive).toBeTruthy();
  });
});
